import React from "react";
import "./Button.css";

export default function Button() {
  return (
    <div>
      <button type="button" className="join btn btn-warning align-items-center">
        join us
      </button>
    </div>
  );
}
